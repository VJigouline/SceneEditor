import { HemisphereLightHelper } from './hemisphere-light-helper';

describe('HemisphereLightHelper', () => {
  it('should create an instance', () => {
    expect(new HemisphereLightHelper()).toBeTruthy();
  });
});
